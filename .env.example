# Redis
REDIS_URL=redis://localhost:6379/0

# Rate limit defaults
RATE_LIMIT_ALGO=fixed_window  # fixed_window | token_bucket
DEFAULT_LIMIT=60              # requests per window (fixed_window)
WINDOW_SECONDS=60

# Token bucket
TB_CAPACITY=30
TB_REFILL_PER_SEC=1.0

# UI
APP_NAME=PulseGuard
